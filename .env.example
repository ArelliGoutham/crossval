MONGODB_URI=mongodb://localhost:27018/?replicaSet=rs0
MONGODB_DB_NAME=crossval
APP_ORIGIN=http://localhost:3000
SESSION_TTL_DAYS=7
BCRYPT_COST=12
BCRYPT_DUMMY_HASH=
# Generate a local dummy hash: node -e "import('bcrypt').then(b => b.hash('dummy-password', 12).then(h => console.log(h)))"
# The BCRYPT_DUMMY_HASH must use the same cost as BCRYPT_COST.
